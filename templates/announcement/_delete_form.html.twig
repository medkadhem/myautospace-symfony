<form method="post" action="{{ path('app_announcement_delete', {'id': announcement.id}) }}" onsubmit="return confirm('Are you sure? This cannot be undone.');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ announcement.id) }}">
    <button class="btn danger btn-lg">Delete Listing Permanently</button>
</form>
