{% extends 'base.html.twig' %}

{% block title %}AnnouncementType index{% endblock %}

{% block body %}
    <h1>AnnouncementType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for announcement_type in announcement_types %}
            <tr>
                <td>{{ announcement_type.id }}</td>
                <td>{{ announcement_type.name }}</td>
                <td>{{ announcement_type.description }}</td>
                <td>
                    <a href="{{ path('app_announcement_type_show', {'id': announcement_type.id}) }}">show</a>
                    <a href="{{ path('app_announcement_type_edit', {'id': announcement_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_announcement_type_new') }}">Create new</a>
{% endblock %}
