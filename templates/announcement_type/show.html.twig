{% extends 'base.html.twig' %}

{% block title %}AnnouncementType{% endblock %}

{% block body %}
    <h1>AnnouncementType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ announcement_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ announcement_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ announcement_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_announcement_type_index') }}">back to list</a>

    <a href="{{ path('app_announcement_type_edit', {'id': announcement_type.id}) }}">edit</a>

    {{ include('announcement_type/_delete_form.html.twig') }}
{% endblock %}
