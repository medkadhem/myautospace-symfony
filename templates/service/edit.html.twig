{% extends 'base.html.twig' %}

{% block title %}Edit Service - MyAutoSpace{% endblock %}

{% block body %}
  {{ include('service/_form.html.twig', {'service': service}) }}

  <div class="dashboard-container" style="margin-top: 20px;">
    <div style="max-width: 800px; margin: 0 auto; padding: 30px; background: rgba(231,76,60,.1); border: 1px solid rgba(231,76,60,.3); border-radius: 12px;">
      <h3 style="margin: 0 0 12px; color: var(--danger);">⚠️ Danger Zone</h3>
      <p style="color: var(--muted); margin: 0 0 16px;">Once you delete this service, there is no going back. Please be certain.</p>
      {{ include('service/_delete_form.html.twig') }}
    </div>
  </div>
{% endblock %}
